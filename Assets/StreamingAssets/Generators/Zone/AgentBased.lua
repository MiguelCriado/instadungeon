require 'utils'

function pre_connect_zones(map, level)

end

function generate(map, level)

end

function post_connect_zones(map, level)

end

function get_spawn_point(map, level)

end

function get_exit_point(map, level)

end
